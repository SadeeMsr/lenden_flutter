import 'package:flutter/material.dart';

class EmailUsage extends StatelessWidget {
  const EmailUsage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Flutter layout demo'),
      ),
      body: const Center(
        child: Text('Hello World'),
      ),
    );
  }
}
